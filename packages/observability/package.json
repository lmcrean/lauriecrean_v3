{
  "name": "@lauriecrean/observability",
  "version": "1.0.0",
  "description": "Observability and logging utilities for lauriecrean project",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "winston": "^3.11.0",
    "chalk": "^4.1.2"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "rimraf": "^5.0.0"
  },
  "peerDependencies": {
    "@playwright/test": "^1.51.0"
  },
  "files": [
    "dist/**/*"
  ],
  "engines": {
    "node": ">=18.0"
  }
} 